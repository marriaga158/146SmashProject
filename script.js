function redTheme(elem){
    if (elem.checked){
        // alert("Im checked"); test code
        // this needs to remove the td elements and add one cal103
        $("body").css("background-color", "red"); // this removes all of the <td> elements that need to be cleared for 103
    }
}

function blueTheme(elem){
    if (elem.checked){
        // alert("Im checked"); test code
        // this needs to remove the td elements and add one cal103
        $("body").css("background-color", "cyan"); // this removes all of the <td> elements that need to be cleared for 103
    }
}

function greenTheme(elem){
    if (elem.checked){
        // alert("Im checked"); test code
        // this needs to remove the td elements and add one cal103
        $("body").css("background-color", "green"); // this removes all of the <td> elements that need to be cleared for 103
    }
}

function pinkTheme(elem){
    if (elem.checked){
        // alert("Im checked"); test code
        // this needs to remove the td elements and add one cal103
        $("body").css("background-color", "pink"); // this removes all of the <td> elements that need to be cleared for 103
    }
}